# 11_작업지시서_v3.6.md
작성일: 2026-07-06
이전 작업지시서: 10_작업지시서_v3.6.md (SOP 탭+편집기 — 실행 보류)
작업 유형: ■ 기능 개선 (SOP HTML 탭 구조 + 섹션 자동 분류)
작업 경로: C:\Obsidian\GuideBook\GuideBook_생성기\

## 변경 이력
- 10_v3.6: 탭 구조 + 자료 분류 코드 편집기 + 폴더 경로 localStorage → 설계 검토 후 보류
- 11_v3.6: 탭 구조 + classify_sections() 유지 / 편집기·localStorage 제거
  → 이유: SOP는 읽기 전용(Documentation) 원칙 유지
           Markdown(SSOT) ↔ localStorage 분리는 데이터 불일치 위험
           편집 기능은 추후 PrintFlow/CheckFlow 관리자 도구(v4.0)에서 구현

---

## 작업 목표 (3가지)

1. SOP HTML에 탭 구조 추가 (📋 가이드 / 📌 업무규칙 / ❓ FAQ)
2. classify_sections() 함수 추가 → guide/rule/faq 섹션 자동 분류
3. FAQ 중복 제거 → guide.md의 자주 하는 실수·FAQ 섹션을 FAQ 탭으로 이동

모든 변경은 generator.py에 반영 → 앞으로 모든 앱 SOP에 자동 적용.
SOP는 읽기 전용 원칙 유지 (편집 기능 없음).

---

## STEP 1. 현재 상태 파악

```
cd C:\Obsidian\GuideBook\GuideBook_생성기

grep -n "generate_sop\|def sop\|md_to_html\|FAQ\|자주" generator.py | head -30
```

---

## STEP 2. generator.py에 classify_sections() 함수 추가

아래 함수를 generator.py 상단 함수 영역에 추가한다.

```python
# ── 섹션 분류 키워드 ──────────────────────────────────────
FAQ_KEYWORDS   = ['자주 하는 실수', 'FAQ', '자주묻는', '오류', '에러', '트러블']
RULE_KEYWORDS  = ['업무 규칙', '분류 코드', '폴더 구조', '파일명 규칙',
                  '저장 원칙', '검사 규칙', '교재 코드', '과정 코드']

def classify_sections(content):
    """
    마크다운 content를 H1/H2 섹션 단위로 파싱하여
    guide_body / rule_body / faq_body 세 파트로 분류한다.

    분류 기준:
    - H1 제목에 RULE_KEYWORDS → rule 파트
    - H1 제목에 FAQ_KEYWORDS  → faq 파트
    - 그 외 H1                → guide 파트
    - H2 제목에 FAQ_KEYWORDS  → faq 파트 (현재 H1 컨텍스트 무관)
    - H2 제목에 RULE_KEYWORDS → rule 파트 (현재 H1 컨텍스트 무관)
    - 그 외 H2                → 현재 H1 컨텍스트 유지
    """
    lines = content.split('\n')
    guide_lines, rule_lines, faq_lines = [], [], []
    current_target = guide_lines
    current_ctx    = 'guide'   # 현재 H1 컨텍스트

    for line in lines:
        stripped = line.strip()

        # H1 처리 (## 로 시작하지 않는 # 으로 시작하는 라인)
        if stripped.startswith('# ') and not stripped.startswith('## '):
            title = stripped[2:].strip()
            if any(k in title for k in RULE_KEYWORDS):
                current_target = rule_lines
                current_ctx    = 'rule'
            elif any(k in title for k in FAQ_KEYWORDS):
                current_target = faq_lines
                current_ctx    = 'faq'
            else:
                current_target = guide_lines
                current_ctx    = 'guide'
            current_target.append(line)

        # H2 처리
        elif stripped.startswith('## '):
            title = stripped[3:].strip()
            if any(k in title for k in FAQ_KEYWORDS):
                current_target = faq_lines
            elif any(k in title for k in RULE_KEYWORDS):
                current_target = rule_lines
            else:
                # H1 컨텍스트 유지
                if current_ctx == 'rule':
                    current_target = rule_lines
                elif current_ctx == 'faq':
                    current_target = faq_lines
                else:
                    current_target = guide_lines
            current_target.append(line)

        else:
            current_target.append(line)

    return (
        '\n'.join(guide_lines).strip(),
        '\n'.join(rule_lines).strip(),
        '\n'.join(faq_lines).strip()
    )
```

---

## STEP 3. generate_sop() 수정

### 3-1. CSS 추가

generate_sop()가 생성하는 HTML의 `<style>` 블록 안에 아래 CSS를 추가한다.

```css
/* ── 탭 구조 ── */
.tab-bar {
  display: flex;
  gap: 0;
  border-bottom: 2px solid #e5e7eb;
  margin: 16px 0 24px;
}
.tab-btn {
  padding: 10px 24px;
  background: none;
  border: none;
  font-size: 14px;
  font-family: inherit;
  cursor: pointer;
  color: #6b7280;
  border-bottom: 3px solid transparent;
  margin-bottom: -2px;
  transition: all 0.15s;
}
.tab-btn:hover { color: #1e40af; }
.tab-btn.active {
  color: #1e40af;
  font-weight: 600;
  border-bottom: 3px solid #3b82f6;
}
.tab-panel { display: none; }
.tab-panel.active { display: block; }
.tab-empty {
  padding: 40px 0;
  text-align: center;
  color: #9ca3af;
  font-size: 14px;
}
```

### 3-2. HTML body 구조 변경

generate_sop()의 HTML body에서
기존 단일 콘텐츠 블록을 아래 탭 구조로 교체한다.

```python
# generate_sop() 안에서:

# 1. 전체 content를 3개 파트로 분류
guide_md, rule_md, faq_md = classify_sections(guide_content)

# 2. 각 파트를 HTML로 변환
guide_html = md_to_html(guide_md) if guide_md else \
    '<div class="tab-empty">내용이 없습니다.</div>'
rule_html  = md_to_html(rule_md)  if rule_md  else \
    '<div class="tab-empty">업무 규칙이 없습니다. rule.md를 작성해주세요.</div>'
faq_html   = md_to_html(faq_md)   if faq_md   else \
    '<div class="tab-empty">FAQ가 없습니다. faq.md를 작성해주세요.</div>'

# 3. 탭 UI 삽입
tab_bar = '''
<div class="tab-bar">
  <button class="tab-btn active" onclick="switchTab('guide', this)">
    📋 가이드
  </button>
  <button class="tab-btn" onclick="switchTab('rule', this)">
    📌 업무규칙
  </button>
  <button class="tab-btn" onclick="switchTab('faq', this)">
    ❓ FAQ
  </button>
</div>

<div id="tab-guide" class="tab-panel active">
''' + guide_html + '''
</div>

<div id="tab-rule" class="tab-panel">
''' + rule_html + '''
</div>

<div id="tab-faq" class="tab-panel">
''' + faq_html + '''
</div>
'''
```

### 3-3. JavaScript 추가

generate_sop()의 `<script>` 블록 안에 아래 JS를 추가한다.

```javascript
// ── 탭 전환 ──────────────────────────────────────────────
function switchTab(name, btn) {
  document.querySelectorAll('.tab-panel').forEach(function(p) {
    p.classList.remove('active');
  });
  document.querySelectorAll('.tab-btn').forEach(function(b) {
    b.classList.remove('active');
  });
  document.getElementById('tab-' + name).classList.add('active');
  btn.classList.add('active');
}
```

---

## STEP 4. 문법 검증 및 PrintFlow SOP 재생성

```
cd C:\Obsidian\GuideBook\GuideBook_생성기

python -c "import ast; ast.parse(open('generator.py', encoding='utf-8').read())" && echo "SYNTAX OK"

python generator.py PrintFlow sop
```

콘솔 출력에서 확인할 것:
```
읽은 파일: guide.md, rule.md, faq.md
```

---

## STEP 5. 브라우저 검증

```
start apps\PrintFlow\output\PrintFlow_SOP_v1.0.html
```

아래 항목을 순서대로 확인:

| # | 확인 항목 | 기대 결과 |
|---|-----------|-----------|
| 1 | 탭 3개 표시 | 📋 가이드 / 📌 업무규칙 / ❓ FAQ |
| 2 | 탭 클릭 전환 | 클릭 시 내용 전환, 활성 탭 파란 밑줄 |
| 3 | 가이드 탭 | 앱 개요~조교 인수인계 내용 표시 |
| 4 | 가이드 탭 | 자주 하는 실수·FAQ 미표시 (FAQ 탭으로 이동됨) |
| 5 | 업무규칙 탭 | 자료 분류 코드, 폴더 구조, 파일명 규칙, 검사 규칙 표시 |
| 6 | FAQ 탭 | 자주 하는 실수 표 + Q&A 표시 (중복 없이 1회만) |
| 7 | 빈 탭 처리 | 내용 없으면 "내용이 없습니다." 안내 표시 |

---

## STEP 6. KingTestMaker / Dooly SOP도 동일 구조 적용 확인

```
python generator.py KingTestMaker sop
python generator.py Dooly sop
```

각각 열어서 탭 3개가 정상 표시되는지 확인.
(rule.md / faq.md가 "(추후 작성)" 상태이면 해당 탭에 안내 메시지 표시 — 정상)

```
start apps\KingTestMaker\output\KingTestMaker_SOP_v1.0.html
start apps\Dooly\output\Dooly_SOP_v1.0.html
```

---

## STEP 7. 오류 발생 시 처리

오류 발생 시 한국어로 원인과 해결 방법 설명.

자주 발생하는 오류:
- f-string 이스케이프: HTML 안에서 `{`, `}` 사용 시 `{{`, `}}` 처리 확인
- classify_sections() 빈 content 처리: strip() 후 빈 문자열 체크
- H1 파싱 오류: `# ` 와 `## ` 구분 확인 (공백 포함)

---

## 완료 후 보고 내용

1. generator.py 변경 내용 요약 (추가된 함수, 수정된 함수)
2. classify_sections() 정상 동작 여부
3. 탭 구조 정상 표시 여부
4. FAQ 중복 제거 결과 (가이드 탭에서 제거됐는지)
5. KingTestMaker / Dooly SOP 탭 구조 적용 여부
6. 특이사항 또는 오류 내용

### 다음 권장 작업
```
□ python generator.py PrintFlow tutorial
  → 튜토리얼은 탭 구조 영향 없는지 확인

□ python analyzer.py KingTestMaker "C:\Obsidian\복테메이커" "docs,electron,html,log"
  python generator.py KingTestMaker both
  → KingTestMaker 실제 내용 가이드북 생성

□ python analyzer.py Dooly "C:\Obsidian\Dooly" "00_System,01_Project,docs,api"
  python generator.py Dooly both
  → Dooly 실제 내용 가이드북 생성
```

