# 17_작업지시서_v3.8.1.md
작성일: 2026-07-07
이전 작업지시서: 14_v3.8 → 15_v3.8.1 → 16_v3.8.1 → 17_v3.8.1 (Regression 판정 로직 단계적 개선)
작업 유형: ■ 버그 수정 (Regression 판정 로직 — Mode-aware + Result/Display 분리 + Summary)
작업 경로: C:\Obsidian\GuideBook\GuideBook_생성기\

Project: GuideBook Generator
App: 전체 (PrintFlow / KingTestMaker / Dooly)
Type: Bug Fix
Priority: High
Status: Ready
Version: v3.8.1

## 변경 이력
- v3.8:   Regression Check 4계층 (Core/Renderer/App/Overall) 추가
- v3.8.1: Mode-aware 판정 + Result/Display 분리 + Summary + N/A 이유 표시

---

## 문제 정의

현재 Regression은 두 가지 문제가 있다.

### 문제 1 — Mode-aware 미지원
```
sop 모드 실행
  → Tutorial HTML 없음 → Core FAIL
  → Overall FAIL
```
의도된 미생성(sop 모드)과 실제 오류를 구분할 수 없다.

### 문제 2 — Result와 Display가 혼재
```python
overall = 'PASS (N/A 포함)'   # ← 판정과 표시가 섞임
```
다른 코드에서 `if overall == 'PASS':` 비교가 깨질 수 있다.
앞으로 PASS (WARNING), PASS (SKIP) 등이 생기면 판정 로직이 복잡해진다.

---

## 목표

### 1. 판정 항목 3종 정의

| 기호 | 판정 | 의미 |
|------|------|------|
| ✓ | PASS | 검사 완료, 정상 |
| ✗ | FAIL | 검사 완료, 이상 |
| - | N/A  | 해당 모드에서 생성 안 함 (정상) |

### 2. Result / Display 분리

```python
# 내부 판정 — 항상 단순하게 PASS / FAIL 만
result = 'PASS' or 'FAIL'

# 표시용 메타데이터 — 별도 관리
result_meta = {
    'pass_count': 18,
    'fail_count': 0,
    'na_count':   3,
    'has_na':     True,
}

# 표시
display = 'PASS (N/A 포함)' if (result == 'PASS' and result_meta['has_na']) else result
```

`if result == 'PASS':` 비교는 항상 안전하게 유지된다.

### 3. N/A 항목에 이유 표시
```
- Tutorial HTML (mode=sop)
- Renderer 검사 (mode=sop, SOP 미생성)
```

### 4. Report 마지막에 Summary 추가
```
## Summary
PASS : 10
FAIL :  0
N/A  :  3
```

### 5. 모드별 N/A 규칙
```
both     → SOP + Tutorial 모두 검사
sop      → Tutorial 항목 → N/A
tutorial → SOP 항목 → N/A
```

---

## STEP 1. 현재 상태 파악

```
cd C:\Obsidian\GuideBook\GuideBook_생성기

grep -n "def run_regression_check\|overall\|core_pass\|rend_pass\|app_pass" generator.py | head -20
```

---

## STEP 2. run_regression_check() 시그니처 변경

`mode` 파라미터를 추가한다.

```python
def run_regression_check(app_name, output_dir, input_dir, generated_files, mode='both'):
    """
    4계층 Regression Check.
    mode: 'both' | 'sop' | 'tutorial'

    판정: ✓ PASS / ✗ FAIL / - N/A
    Result(판정)와 Display(표시)는 분리한다.
    """
```

---

## STEP 3. check() 헬퍼 교체 — N/A 지원

기존 check() 함수를 아래로 교체한다.

```python
def check(results, fails, na_list, label, condition,
          fail_msg=None, na=False, na_reason=''):
    """
    na=True  → N/A 처리. na_reason으로 이유 표시.
    na=False → 정상 PASS/FAIL 판정.
    """
    if na:
        reason = f' ({na_reason})' if na_reason else ''
        results.append(f'- {label}{reason}')
        na_list.append(label)
    elif condition:
        results.append(f'✓ {label}')
    else:
        results.append(f'✗ {label}')
        fails.append(fail_msg or label)
```

각 계층의 check() 호출부에 `na_list` 인자를 추가해야 한다.
계층별로 `core_na`, `rend_na`, `app_na` 리스트를 선언한다.

```python
core_results, core_fails, core_na = [], [], []
rend_results, rend_fails, rend_na = [], [], []
app_results,  app_fails,  app_na  = [], [], []
```

---

## STEP 4. Core Regression — mode-aware 적용

```python
tutorial_na = (mode == 'sop')
sop_na      = (mode == 'tutorial')

check(core_results, core_fails, core_na,
      'guide.md 읽기',
      input_mtimes['guide.md'] != '파일 없음', 'guide.md 없음')
check(core_results, core_fails, core_na,
      'rule.md 읽기',
      input_mtimes['rule.md']  != '파일 없음', 'rule.md 없음')
check(core_results, core_fails, core_na,
      'faq.md 읽기',
      input_mtimes['faq.md']   != '파일 없음', 'faq.md 없음')
check(core_results, core_fails, core_na,
      'SOP HTML 생성',
      sop_html and os.path.exists(sop_html), 'SOP HTML 생성 실패',
      na=sop_na, na_reason=f'mode={mode}')
check(core_results, core_fails, core_na,
      'SOP MD 생성',
      sop_md   and os.path.exists(sop_md),   'SOP MD 생성 실패',
      na=sop_na, na_reason=f'mode={mode}')
check(core_results, core_fails, core_na,
      'Tutorial HTML 생성',
      tutorial_html and os.path.exists(tutorial_html), 'Tutorial HTML 생성 실패',
      na=tutorial_na, na_reason=f'mode={mode}')

core_pass = not core_fails
```

---

## STEP 5. Renderer Regression — mode-aware 적용

```python
rend_results, rend_fails, rend_na = [], [], []

if sop_na:
    # tutorial 모드: SOP 미생성이므로 Renderer 전체 N/A
    for label in ['코드블록 렌더링 (<pre><code>)', '수평선 렌더링 (<hr>)',
                  'H4 렌더링 (<h4>)', '인라인 코드 렌더링 (<code>)']:
        check(rend_results, rend_fails, rend_na, label, False,
              na=True, na_reason=f'mode={mode}, SOP 미생성')
elif sop_html and os.path.exists(sop_html):
    html = open(sop_html, encoding='utf-8').read()
    check(rend_results, rend_fails, rend_na,
          '코드블록 렌더링 (<pre><code>)',
          '<pre><code>' in html, 'SOP에 <pre><code> 없음')
    check(rend_results, rend_fails, rend_na,
          '수평선 렌더링 (<hr>)',
          '<hr '        in html, 'SOP에 <hr> 없음')
    check(rend_results, rend_fails, rend_na,
          'H4 렌더링 (<h4>)',
          '<h4'         in html, 'SOP에 <h4> 없음')
    check(rend_results, rend_fails, rend_na,
          '인라인 코드 렌더링 (<code>)',
          '<code>'      in html, 'SOP에 인라인 <code> 없음')
else:
    rend_results.append('- SOP HTML 없어 Renderer 검사 건너뜀')

rend_pass = not rend_fails
```

---

## STEP 6. App Regression — na_list 인자 추가

APP_CHECKS 구조는 그대로 유지.
check() 호출 시 `app_na` 인자만 추가한다.

```python
app_results, app_fails, app_na = [], [], []

app_spec = APP_CHECKS.get(app_name, [])
if app_spec and sop_html and os.path.exists(sop_html):
    html = open(sop_html, encoding='utf-8').read()
    for label, fn, fail_msg in app_spec:
        check(app_results, app_fails, app_na, label, fn(html), fail_msg)
elif sop_na:
    for label, _, _ in app_spec:
        check(app_results, app_fails, app_na, label, False,
              na=True, na_reason=f'mode={mode}, SOP 미생성')
elif not app_spec:
    app_results.append(f'- {app_name} 앱 전용 검사 항목 미정의 (건너뜀)')
else:
    app_results.append('- SOP HTML 없어 App 검사 건너뜀')

app_pass = not app_fails
```

---

## STEP 7. Overall — Result / Display 분리

```python
# ── Result: 항상 PASS / FAIL 만 ──────────────────────────
all_fails = core_fails + rend_fails + app_fails
result    = 'FAIL' if all_fails else 'PASS'

# ── Metadata ──────────────────────────────────────────────
all_na      = core_na + rend_na + app_na
all_results = core_results + rend_results + app_results
pass_count  = sum(1 for r in all_results if r.startswith('✓'))
fail_count  = len(all_fails)
na_count    = len(all_na)

result_meta = {
    'pass_count': pass_count,
    'fail_count': fail_count,
    'na_count':   na_count,
    'has_na':     na_count > 0,
    'display':    'PASS (N/A 포함)' if (result == 'PASS' and na_count > 0) else result,
}

# display는 result_meta에서 파생
display = result_meta['display']
```

이후 터미널 출력과 Report에서 `result` (판정)와 `display` (표시)를 구분해서 사용한다.
YAML 메타데이터의 `Result:` 필드는 `result` (PASS/FAIL) 를 사용한다.

---

## STEP 8. 터미널 출력 — Summary 추가

Overall 출력 부분에 Summary를 추가한다.

```python
print(f'\n{sep}')
print(f'  Overall : {display}')
if result == 'FAIL':
    for f in all_fails:
        print(f'    - {f}')
print(f'\n  Summary')
print(f'    PASS : {result_meta["pass_count"]:>3}')
print(f'    FAIL : {result_meta["fail_count"]:>3}')
print(f'    N/A  : {result_meta["na_count"]:>3}')
print(f'\n  Report  : {report_path}')
print(f'{sep}\n')
```

---

## STEP 9. Regression_Report.md — Summary 섹션 추가

Report 마지막 `## 다음 권장 작업` 앞에 Summary 섹션을 추가한다.

```python
md += ['---', '']
md += ['## Summary', '']
md += [f'| 항목 | 수 |']
md += [f'|------|-----|']
md += [f'| ✓ PASS | {result_meta["pass_count"]} |']
md += [f'| ✗ FAIL | {result_meta["fail_count"]} |']
md += [f'| - N/A  | {result_meta["na_count"]} |']
md += ['']
```

YAML 헤더의 `Result:` 는 `result` (PASS/FAIL) 사용.
Overall 섹션의 표시는 `display` 사용.

---

## STEP 10. main()에서 mode 전달

```python
run_regression_check(
    app_name=app_name,
    output_dir=output_dir,
    input_dir=input_dir,
    generated_files=generated_files,
    mode=mode,          # ← 추가
)
```

---

## STEP 11. 문법 검증 및 3개 앱 재실행

```
cd C:\Obsidian\GuideBook\GuideBook_생성기

python -c "import ast; ast.parse(open('generator.py', encoding='utf-8').read())" && echo "SYNTAX OK"

python generator.py PrintFlow both
python generator.py KingTestMaker sop
python generator.py Dooly sop
```

---

## STEP 12. 결과 확인 기준

| 실행 | 기대 Result | 기대 Display | N/A 항목 |
|------|------------|--------------|----------|
| `PrintFlow both` | PASS | PASS | 0개 |
| `KingTestMaker sop` | PASS | PASS (N/A 포함) | Tutorial + Renderer 항목 |
| `Dooly sop` | PASS | PASS (N/A 포함) | Tutorial + Renderer 항목 |

Regression_Report.md YAML 헤더의 `Result:` 는 항상 `PASS` 또는 `FAIL` 만 기록되는지 확인.

---

## STEP 13. 오류 발생 시 처리

자주 발생하는 오류:
- `na_list` 인자 누락: check() 호출부 3번째 인자에 `core_na` / `rend_na` / `app_na` 추가 확인
- `result` vs `display` 혼용: YAML 헤더는 `result`, 터미널/Report 표시는 `display` 확인
- `result_meta` 미정의: STEP 7 코드 전체가 Overall 섹션 앞에 위치하는지 확인

---

## 완료 후 보고 내용

1. check() 헬퍼 변경 내용 (na_list 인자 추가)
2. Result / Display 분리 위치 (generator.py 라인)
3. PrintFlow both — Overall result/display 값
4. KingTestMaker sop — N/A 항목 목록 + 이유 표시 확인
5. Dooly sop — 동일
6. Summary 출력 (터미널 + Report) 확인
7. YAML 헤더 `Result:` 가 PASS/FAIL 만 기록되는지 확인
8. 특이사항

### 다음 권장 작업
```
□ 18_작업지시서_v3.9: PDF 전체 출력 개선
  → 인쇄 시 Guide / Rule / FAQ 모두 출력
  → @media print { .tab-panel { display: block !important; } }

□ Phase B: KingTestMaker analyzer.py 실행
  python analyzer.py KingTestMaker "C:\Obsidian\복테메이커" "docs,electron,html,log"
  python generator.py KingTestMaker both

□ Phase B: Dooly analyzer.py 실행
  python analyzer.py Dooly "C:\Obsidian\Dooly" "00_System,01_Project,docs,api"
  python generator.py Dooly both
```
