# 시행착오 기록부
GuideBook Generator 프로젝트 — 에러·버그·원인·해결방법 누적 기록
저장 위치: C:\Obsidian\GuideBook\99_공통\시행착오_기록.md

---

## 기록 요약

| 번호 | 날짜 | 분류 | 제목 | 상태 |
|------|------|------|------|------|
| 001 | 2026-07-06 | 설계 | BAT 방식 vs PWA 방식 결정 | ✅ 해결 |
| 002 | 2026-07-06 | 설계 | 폴더명 GuideBook_Generator → GuideBook 단축 | ✅ 해결 |
| 003 | 2026-07-06 | 설계 | BAT 정적 메뉴 → apps/ 자동 스캔 방식 전환 | ✅ 해결 |
| 004 | 2026-07-06 | 버그 | SOP HTML 마크다운 표가 텍스트로 출력 | ✅ 해결 |
| 005 | 2026-07-06 | 버그 | 튜토리얼 HTML 역할 탭 없이 전체 STEP 출력 | ✅ 해결 |
| 006 | 2026-07-06 | 버그 | tutorialPDF f-string 치환 안 됨 | ✅ 해결 |
| 007 | 2026-07-06 | 설계 | SOP 인쇄 버튼 위치 하단 → 상단 이동 | ✅ 해결 |
| 008 | 2026-07-07 | 설계 | preprocess_code_blocks() → 상태머신 방식 전환 | ✅ 해결 |
| 009 | 2026-07-07 | 버그 | Windows 콘솔 cp949 인코딩 오류 | ✅ 해결 |
| 010 | 2026-07-07 | 운영 | Dooly Regression FAIL → H4 콘텐츠 부재 | ✅ 해결 |
| 011 | 2026-07-09 | 운영 | guide 최소화 후 Renderer FAIL → 조건부 N/A 정책 도입 | ✅ 해결 |

---

## 상세 기록

### #001 — BAT 방식 vs PWA 방식 결정
| 항목 | 내용 |
|------|------|
| 날짜 | 2026-07-06 |
| 분류 | 설계 결정 |
| 상태 | ✅ 해결 |

**상황**: BAT 파일 방식 vs PWA 방식 검토.
**결정**: BAT + Python 채택 (학원 Windows 환경에 적합). 나중에 PWA 확장 가능 구조 유지.
**교훈**: 배포 환경을 먼저 확인하고 방식 결정할 것.

---

### #002 — 폴더명 단축
| 항목 | 내용 |
|------|------|
| 날짜 | 2026-07-06 |
| 분류 | 설계 결정 |
| 상태 | ✅ 해결 |

**상황**: 초기 폴더명 `GuideBook_Generator`가 너무 길어서 불편.
**해결**: `C:\Obsidian\GuideBook\`으로 단축. 내부 구조 동일 유지.
**영향**: 모든 경로 참조 일괄 수정 필요.

---

### #003 — BAT 정적 메뉴 → 동적 스캔 방식 전환
| 항목 | 내용 |
|------|------|
| 날짜 | 2026-07-06 |
| 분류 | 설계 결정 |
| 상태 | ✅ 해결 |

**상황**: 앱 이름 하드코딩 → 앱 추가 시마다 BAT 수정 필요.
**해결**: apps/ 폴더 자동 스캔으로 메뉴 번호 동적 생성.
**구현**:
```bat
setlocal enabledelayedexpansion
for /d %%d in (apps\*) do (set /a count+=1 & set "app[!count!]=%%~nxd")
```
**교훈**: 추가될 항목이 있는 메뉴는 처음부터 동적 생성 방식으로 설계.

---

### #004 — SOP HTML 표 깨짐
| 항목 | 내용 |
|------|------|
| 날짜 | 2026-07-06 |
| 분류 | 버그 |
| 상태 | ✅ 해결 |

**증상**: "자주 하는 실수" 마크다운 표가 텍스트 그대로 출력됨.
**원인**: generator.py 변환 루프에 표(`|`로 시작) 처리 로직 없음.
**해결**: `md_to_html()` 함수 신규 작성.
- `|` 시작 줄 → table/th/td 변환
- `|---|` 구분선 행 자동 건너뜀
- `in_table` 플래그로 표 시작/종료 감지

**수정 파일**: `generator.py` — `generate_sop()`

---

### #005 — 튜토리얼 역할 탭 없음
| 항목 | 내용 |
|------|------|
| 날짜 | 2026-07-06 |
| 분류 | 버그 |
| 상태 | ✅ 해결 |

**증상**: 강사/조교 탭 없이 ## 섹션 전체(13개)가 하나의 STEP 목록으로 출력.
**원인**: `generate_tutorial()`이 섹션 제목 키워드를 분류하지 않음.
**해결**: 섹션 제목 키워드로 자동 분류.
- "강사", "teacher", "강사k" → 강사 탭
- "조교", "assistant", "조교p" → 조교 탭
- 그 외 → 공통 (구분 없는 앱은 단일 목록)
- 조교 탭 우선 표시

**수정 파일**: `generator.py` — `generate_tutorial()` 전면 재작성

---

### #006 — tutorialPDF f-string 치환 안 됨
| 항목 | 내용 |
|------|------|
| 날짜 | 2026-07-06 |
| 분류 | 버그 |
| 상태 | ✅ 해결 |

**증상**: `tutorialPDF()` 안에서 `{app_name}` 리터럴 그대로 출력될 가능성.
**원인**: `role_js`가 일반 문자열(str)로 정의되어 f-string 치환 미적용.
**해결**: Claude Code가 자체 감지하여 `role_js = f"""..."""` 형태로 수정.
**교훈**:
- Python f-string 안 JS 코드에서 `{ }` 사용 시 주의
- JS 중괄호: `{{ }}` 이스케이프
- Python 변수 치환: `{변수명}` 그대로
- 변수 삽입 필요한 문자열은 반드시 `f"""..."""` 선언

---

### #007 — SOP 인쇄 버튼 위치 개선
| 항목 | 내용 |
|------|------|
| 날짜 | 2026-07-06 |
| 분류 | 설계 개선 |
| 상태 | ✅ 해결 |

**상황**: SOP HTML 인쇄 버튼이 본문 맨 하단 → 긴 문서에서 버튼 접근 불편.
**해결**: 상단 sticky 헤더에 버튼 배치.
- `position:sticky; top:0; z-index:10` — 스크롤해도 상단 고정
- 왼쪽: 앱 이름 + 생성일 / 오른쪽: 인쇄 + PDF 버튼
- `@media print` — 인쇄 시 헤더 버튼 숨김

**결과**: SOP와 튜토리얼 HTML이 동일한 상단 버튼 구조를 가지게 됨.

---

### #008 — preprocess_code_blocks() 정규식 방식 → 상태머신 전환
| 항목 | 내용 |
|------|------|
| 날짜 | 2026-07-07 |
| 분류 | 설계 개선 (구현 개선 허용 원칙 적용) |
| 상태 | ✅ 해결 |

**작업지시서 설계**: ` ``` ` 블록을 정규식(`re.sub`)으로 먼저 `<pre><code>`로 변환 후 줄 단위 처리.
**문제점**: 정규식으로 HTML 변환 후 다시 `split("\n")`으로 분할 시 멀티라인 코드블록이 줄 단위로 쪼개져 각 줄이 `<p>`로 감싸지는 버그 발생.
**Claude Code 판단**: 작업지시서 스펙보다 더 안정적인 방식으로 자체 변경.
**해결**: 줄 단위 루프 내 **상태머신** 방식으로 구현.
- ` ``` ` 펜스 감지 → `in_code` 플래그 ON
- 코드 내용 `code_buf`에 누적
- 닫는 ` ``` ` 감지 → `<pre><code>...</code></pre>` 출력
- HTML 특수문자(`&`, `<`, `>`) 이스케이프 처리

**결과**: 멀티라인 코드블록 정상 렌더링, 잔존 펜스 0개.
**교훈**:
- 작업지시서 설계보다 더 좋은 구현이 있으면 변경 가능
- 단, 동작은 동일해야 하며 변경 이유를 완료 보고에 반드시 기록
- 이 원칙이 `구현 개선 허용 원칙`으로 SYSTEM_PROMPT에 공식화됨

---

### #009 — Windows 콘솔 cp949 인코딩 오류
| 항목 | 내용 |
|------|------|
| 날짜 | 2026-07-07 |
| 분류 | 버그 |
| 상태 | ✅ 해결 |

**증상**: Regression Check 터미널 출력 시 `UnicodeEncodeError: 'cp949' codec can't encode character '\u2014'`.
**원인**: Windows 콘솔 기본 인코딩 cp949가 `—`(em-dash), `✓`, `✗` 등 유니코드 문자를 인코딩하지 못함.
**해결**: `main()` 시작부에 아래 코드 추가.
```python
import sys
sys.stdout.reconfigure(encoding='utf-8')
```
**참고**: Report 파일은 원래 `encoding='utf-8'`으로 저장되어 영향 없음. 터미널 출력만 문제.
**교훈**: Windows 환경에서 유니코드 특수문자(em-dash, 체크마크 등) 터미널 출력 시 반드시 인코딩 설정 확인.

---

### #010 — Dooly Regression FAIL → H4 콘텐츠 부재
| 항목 | 내용 |
|------|------|
| 날짜 | 2026-07-07 |
| 분류 | 운영 (콘텐츠 이슈) |
| 상태 | ✅ 해결 |

**증상**:
```
Dooly generator.py both 실행
→ Renderer FAIL
→ H4 렌더링 (<h4>) 없음
→ Overall FAIL (13/1/0)
```

**원인 분석**:
- Generator 버그 ❌
- `guide.md`에 `####` H4 계층 없음 ✅
- Dooly 소스 문서에 `####` 헤딩이 전무하여 생성된 SOP HTML에 `<h4>` 태그 미생성
- 코드블록·hr·인라인코드는 모두 PASS → Renderer 자체는 정상

**해결**:
```
apps/Dooly/input/guide.md
기존 ### 04_PROJECT_GUIDE_v1.0.md 섹션 내
의미상 자연스러운 위치에

#### MVP 구성
#### 스택 구성 요약

2개 추가 (+28자, 내용 변경 없음)
```

**결과**: Renderer PASS ✅ / Overall PASS ✅ (14/0/0)

**교훈**:
> Regression FAIL은 Generator(코드) 문제가 아니라 입력 콘텐츠(guide.md) 문제일 수 있다.

진단 순서:
1. FAIL 항목 확인
2. 같은 계층(Renderer)의 다른 항목 PASS 여부 확인
   - 다른 항목 PASS → Renderer 정상 → **콘텐츠 부재** 가능성
   - 다른 항목도 FAIL → Generator 버그 가능성
3. 콘텐츠 최소 보강 → 재생성 → PASS 확인

---

### #011 — guide 최소화 후 Renderer FAIL → 조건부 N/A 정책 도입
| 항목 | 내용 |
|------|------|
| 날짜 | 2026-07-09 |
| 분류 | 운영 |
| 상태 | ✅ 해결 |

**문제**: guide 최소화 후 KingTestMaker/Dooly Renderer FAIL 발생.

**원인**: 입력 원문에 코드블록/H4/인라인코드가 없는데도 Renderer가 필수 태그로 검사함.

**해결**:
- 입력 원문에 해당 문법이 없으면 N/A 처리
- 입력에 있는데 출력에 없으면 FAIL 유지
- 수평선(`<hr>`)은 기존처럼 항상 검사

**결과**:
```
PrintFlow     PASS 15 / 0 / 1
KingTestMaker PASS 11 / 0 / 4
Dooly         PASS 11 / 0 / 4
```

**적용 범위:** GuideBook Generator v3.10 이후 / PrintFlow · KingTestMaker · Dooly · CheckFlow 전체 앱

**교훈**:
> Regression은 콘텐츠 존재 여부를 고려해야 한다.
> 없는 콘텐츠는 FAIL이 아니라 N/A다.

---

## 미해결 / 진행 중

| 번호 | 날짜 | 제목 | 현황 |
|------|------|------|------|
| — | — | 현재 미해결 항목 없음 | — |

---

## 새 항목 추가 방법

채팅창에 요청:
```
시행착오 기록 추가해줘.
오류: [내용]
해결: [해결 방법 또는 미해결]
```
