# 20_작업지시서_v1.1.md
# PrintFlow 콘텐츠 리팩터링

작성일: 2026-07-10
작업 대상: guide_PrintFlow.md / rule_PrintFlow.md
작업 성격: 콘텐츠 리팩터링 (기능 변경 없음)

---

## 작업 목표

이번 작업은 문서 역할 분리를 위한 리팩터링이다.

내용을 삭제하는 것이 아니라,
각 문서가 하나의 책임만 갖도록 재배치한다.

Generator 기능은 수정하지 않는다.

---

## 작업 범위

### 1. guide_PrintFlow.md — 소폭 조정

변경 내용:
- 학생 등록 섹션을 "참고" 성격으로 위치 조정
- 업무 중심 구조 유지
- 나머지 내용 변경 없음

조정 전:
```
기본 설정
학생 등록        ← 현재 위치
강사(K) 사용법
조교(P·Q·R) 사용법
조교 간 인수인계
```

조정 후:
```
기본 설정
강사(K) 사용법
조교(P·Q·R) 사용법
조교 간 인수인계
학생 등록        ← 맨 아래 "참고" 섹션으로 이동
```

---

### 2. rule_PrintFlow.md — 역할 분리

변경 내용:
- 운영 규칙 3개 유지 (파일 저장 원칙 / 파일명 규칙 / 검사 규칙)
- 코드표 3개를 rule에서 제거하고 별도 문서로 보존

유지할 내용:
```
파일 저장 원칙 (8가지)
스캔 후 파일명 규칙 (숙제 / 테스트)
검사 규칙 (A. 숙제 검사 / B. 테스트 검사)
```

별도 보존할 내용:
```
자료 분류 코드 (01~09)
폴더 구조 표준 / 과정 코드
교재 코드
```

보존 위치:
```
GuideBook_생성기/apps/PrintFlow/input/reference_PrintFlow.md
```

---

### 3. faq_PrintFlow.md — 변경 없음

---

## 코드표 처리 원칙

코드표(자료 분류 코드 / 과정 코드 / 교재 코드)는 삭제하지 않는다.

```
rule_PrintFlow.md 에서 제거
        ↓
reference_PrintFlow.md 로 이동 (보존)
        ↓
Generator optional 입력
현재는 SOP/Tutorial 생성에 사용하지 않음
        ↓
v2.x Reference 정책에서 출력 활용 예정
```

---

## 작업 순서

1. reference_PrintFlow.md 생성
   → rule의 코드표 3개 이동 (내용 그대로 복사)

2. rule_PrintFlow.md 수정
   → 코드표 3개 섹션 제거
   → 운영 규칙 3개만 남김

3. guide_PrintFlow.md 수정
   → 학생 등록 섹션 위치 조정

4. reference_PrintFlow.md 생성 확인
   → 파일 존재 및 내용 정상 여부 확인

5. Regression 실행
   → PrintFlow PASS 확인

---

## Exit Criteria

- [ ] guide는 신규 직원이 5분 안에 업무 시작 가능
- [ ] rule은 운영 규칙만 포함
- [ ] faq는 변경 없음
- [ ] guide / rule / faq 중복 없음
- [ ] 코드표는 reference_PrintFlow.md로 보존
- [ ] Generator 수정 없음
- [ ] Regression PASS 유지

---

## 제약 조건

- Feature Freeze 유지
- generator.py / analyzer.py 수정 금지
- SSOT 원칙 유지 (내용 삭제 금지, 역할 분리만 수행)
- Generator Regression 기준은 변경하지 않는다.
