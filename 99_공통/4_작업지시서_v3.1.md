# 작업지시서
파일명: 4_작업지시서_v3.1.md
작성일: 2026-07-06
버전: v3.1 (소규모 수정 — generator.py 버그 2개 수정)
수정 내용:
  - 버그①: SOP HTML — 마크다운 표(|---|)가 텍스트로 출력되는 문제
  - 버그②: 튜토리얼 HTML — 역할 탭(강사/조교) 없이 ##섹션 전체가 STEP으로 출력되는 문제

---

## 사전 확인

```
cd C:\Obsidian\GuideBook\GuideBook_생성기
python --version
```

---

## Claude Code 입력 프롬프트

아래 전체를 복사 → Claude Code 입력창에 붙여넣기

---

```
다음 작업을 실행해줘.
작업 경로: C:\Obsidian\GuideBook\GuideBook_생성기\

## 수정할 파일
C:\Obsidian\GuideBook\GuideBook_생성기\generator.py

## 수정 내용 1 — generate_sop() 함수: 마크다운 표 → HTML <table> 변환

현재 generate_sop()의 마크다운→HTML 변환 로직에 표 처리가 없어서
"| 증상 | 원인 |" 같은 내용이 텍스트 그대로 출력된다.

아래 로직으로 교체해줘.

기존 변환 루프 (lines를 순서대로 처리하는 for 루프 전체)를 삭제하고
아래 함수로 교체:

def md_to_html(text):
    """마크다운 → HTML 변환 (표, 제목, 리스트, 경고 지원)"""
    lines = text.split("\n")
    html = ""
    in_table = False
    in_list = False

    for line in lines:
        # 표 행 감지
        if line.startswith("|"):
            stripped = line.strip()
            # 구분선 행(|---|---|) 건너뜀
            if all(c in "|-: " for c in stripped.replace("|", "")):
                if not in_table:
                    html += "<table>\n"
                    in_table = True
                continue
            cells = [c.strip() for c in stripped.strip("|").split("|")]
            if not in_table:
                html += "<table>\n<tr>"
                html += "".join(f"<th>{c}</th>" for c in cells)
                html += "</tr>\n"
                in_table = True
            else:
                html += "<tr>" + "".join(f"<td>{c}</td>" for c in cells) + "</tr>\n"
            continue
        else:
            if in_table:
                html += "</table>\n"
                in_table = False

        # 리스트
        if line.startswith("- "):
            if not in_list:
                html += "<ul>\n"
                in_list = True
            html += f"<li>{line[2:]}</li>\n"
            continue
        else:
            if in_list:
                html += "</ul>\n"
                in_list = False

        # 제목
        if line.startswith("# "):
            html += f"<h1>{line[2:]}</h1>\n"
        elif line.startswith("## "):
            html += f"<h2>{line[3:]}</h2>\n"
        elif line.startswith("### "):
            html += f"<h3>{line[4:]}</h3>\n"
        # 경고
        elif line.startswith("> ⚠️") or line.startswith("> "):
            html += f'<div class="warning">{line.lstrip("> ")}</div>\n'
        # 빈 줄
        elif line.strip() == "":
            html += "<br>\n"
        # 일반 텍스트
        else:
            html += f"<p>{line}</p>\n"

    # 미닫힌 태그 처리
    if in_table:
        html += "</table>\n"
    if in_list:
        html += "</ul>\n"
    return html

그리고 generate_sop() 안에서 기존 변환 루프 대신 이 함수를 호출:
body = md_to_html(guide_content)


## 수정 내용 2 — generate_tutorial() 함수: 역할 탭 분리

현재는 guide.md의 ## 섹션 전체를 하나의 STEP 목록으로 만든다.
guide.md에 아래 키워드가 포함된 섹션을 기준으로 강사/조교 탭을 분리한다.

분리 기준:
- "강사" 또는 "K" 또는 "teacher" 가 제목에 포함된 ## 섹션 → 강사 탭
- "조교" 또는 "P" 또는 "R" 또는 "assistant" 가 제목에 포함된 ## 섹션 → 조교 탭
- 그 외 ## 섹션 (앱 개요, 업무 흐름, 자주 하는 실수, FAQ 등) → 공통 탭

탭이 분리되지 않는 경우(강사/조교 구분 없는 앱):
- 역할 탭 없이 기존 방식으로 단일 STEP 목록 생성

수정된 generate_tutorial() 전체 로직:

def generate_tutorial(app_name, guide_content, images):
    output_dir = get_base_dir() / "apps" / app_name / "output"
    output_dir.mkdir(parents=True, exist_ok=True)
    today = datetime.now().strftime("%Y-%m-%d")

    # ## 섹션 파싱
    sections = []
    current = None
    for line in guide_content.split("\n"):
        if line.startswith("## "):
            if current:
                sections.append(current)
            current = {"title": line[3:].strip(), "content": []}
        elif current:
            current["content"].append(line)
    if current:
        sections.append(current)
    if not sections:
        sections = [{"title": f"{app_name} 사용법",
                     "content": guide_content.split("\n")}]

    # 역할 탭 분류
    teacher_kw = ["강사", "teacher", "강사(k)", "강사k"]
    assistant_kw = ["조교", "assistant", "조교(p", "조교p"]

    teacher_secs = []
    assistant_secs = []
    common_secs = []

    for sec in sections:
        t = sec["title"].lower()
        if any(k in t for k in teacher_kw):
            teacher_secs.append(sec)
        elif any(k in t for k in assistant_kw):
            assistant_secs.append(sec)
        else:
            common_secs.append(sec)

    has_roles = bool(teacher_secs or assistant_secs)

    # 이미지 태그 생성
    def img_tag(idx):
        if idx < len(images):
            import base64
            with open(images[idx], "rb") as f:
                b64 = base64.b64encode(f.read()).decode()
            ext = images[idx].suffix[1:]
            return (f'<img src="data:image/{ext};base64,{b64}" '
                    f'alt="화면 {idx+1}" '
                    f'style="max-width:100%;border-radius:8px;'
                    f'border:1px solid #e5e7eb;">')
        return (f'<div class="mock-screen">📸 STEP {idx+1} 화면'
                f'<br><small>images/ 폴더에 이미지를 추가하면 '
                f'여기에 표시됩니다</small></div>')

    # STEP HTML 블록 생성
    def make_steps(secs, prefix, img_offset=0):
        html = ""
        total = len(secs)
        for i, sec in enumerate(secs):
            content_html = ""
            for line in sec["content"]:
                if line.startswith("- "):
                    content_html += f"<li>{line[2:]}</li>"
                elif line.startswith("> ⚠️") or line.startswith("> "):
                    content_html += (f'<div class="warning">'
                                     f'{line.lstrip("> ")}</div>')
                elif line.strip():
                    content_html += f"<p>{line}</p>"
            disp = "block" if i == 0 else "none"
            pct = int((i + 1) / total * 100) if total else 100
            prev_dis = "disabled" if i == 0 else ""
            next_dis = "disabled" if i == total - 1 else ""
            html += f"""
<div class="{prefix}step" id="{prefix}step-{i}" style="display:{disp}">
  <div class="step-num">STEP {i+1} / {total}</div>
  <h2>{sec['title']}</h2>
  <div class="progress-bar">
    <div class="progress-fill" style="width:{pct}%"></div>
  </div>
  <div class="screen-area">{img_tag(img_offset + i)}</div>
  <div class="content">{content_html}</div>
  <div class="checkpoint">✅ 이 화면이 보이면 다음 단계로 진행하세요.</div>
  <div class="nav">
    <button onclick="{prefix}go({i-1})" {prev_dis}>← 이전</button>
    <span>{i+1} / {total}</span>
    <button onclick="{prefix}go({i+1})" {next_dis}>다음 →</button>
  </div>
</div>"""
        return html

    # 탭 분리 여부에 따라 HTML 구성
    if has_roles:
        # 조교 탭 우선 (조교 먼저 보여줌)
        a_secs = assistant_secs if assistant_secs else common_secs
        t_secs = teacher_secs if teacher_secs else common_secs
        panel_html = f"""
  <div class="role-tabs">
    <div class="role-tab active" onclick="switchRole('a')"
         id="tab-a">조교용</div>
    <div class="role-tab" onclick="switchRole('t')"
         id="tab-t">강사용</div>
  </div>
  <div class="role-panel active" id="panel-a">
    {make_steps(a_secs, 'a')}
  </div>
  <div class="role-panel" id="panel-t">
    {make_steps(t_secs, 't', img_offset=len(a_secs))}
  </div>"""
        role_css = """
  .role-tabs{display:flex;gap:8px;margin-bottom:24px;
             border-bottom:1px solid #e5e7eb}
  .role-tab{flex:1;padding:12px;text-align:center;cursor:pointer;
            font-size:15px;font-weight:600;color:#9ca3af;
            border-bottom:3px solid transparent}
  .role-tab.active{color:#1e3a5f;border-bottom-color:#3b82f6}
  .role-panel{display:none}
  .role-panel.active{display:block}"""
        role_js = """
function switchRole(r){
  document.getElementById('tab-a').classList.toggle('active',r==='a');
  document.getElementById('tab-t').classList.toggle('active',r==='t');
  document.getElementById('panel-a').classList.toggle('active',r==='a');
  document.getElementById('panel-t').classList.toggle('active',r==='t');
}
function ago(n){
  var s=document.querySelectorAll('.astep');
  if(n<0||n>=s.length)return;
  s.forEach(function(x){x.style.display='none'});
  s[n].style.display='block';window.scrollTo(0,0);
}
function tgo(n){
  var s=document.querySelectorAll('.tstep');
  if(n<0||n>=s.length)return;
  s.forEach(function(x){x.style.display='none'});
  s[n].style.display='block';window.scrollTo(0,0);
}"""
    else:
        panel_html = make_steps(sections, 's')
        role_css = ""
        role_js = """
function sgo(n){
  var s=document.querySelectorAll('.sstep');
  if(n<0||n>=s.length)return;
  s.forEach(function(x){x.style.display='none'});
  s[n].style.display='block';window.scrollTo(0,0);
}"""

    html_path = output_dir / f"{app_name}_튜토리얼_v1.0.html"
    backup_file(html_path)
    html_path.write_text(f"""<!DOCTYPE html>
<html lang="ko"><head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>{app_name} 튜토리얼</title>
<style>
  *{{box-sizing:border-box}}
  body{{font-family:'Malgun Gothic',sans-serif;background:#f9fafb;
        margin:0;padding:20px;color:#1f2937}}
  .container{{max-width:720px;margin:0 auto;background:white;
              border-radius:16px;padding:32px;
              box-shadow:0 4px 6px rgba(0,0,0,.07)}}
  {role_css}
  .step-num{{font-size:13px;color:#6b7280;font-weight:600;
             letter-spacing:.05em;margin-bottom:8px}}
  h2{{color:#1e3a5f;margin:0 0 16px;font-size:22px}}
  .progress-bar{{background:#e5e7eb;border-radius:99px;
                 height:6px;margin-bottom:24px}}
  .progress-fill{{background:#3b82f6;height:100%;
                  border-radius:99px;transition:width .3s}}
  .screen-area{{background:#f3f4f6;border-radius:12px;
                min-height:200px;display:flex;
                align-items:center;justify-content:center;
                margin-bottom:24px;padding:16px;text-align:center}}
  .mock-screen{{color:#9ca3af;font-size:14px}}
  .content p,.content li{{font-size:15px;line-height:1.7;margin:6px 0}}
  .content ul{{padding-left:20px;margin:8px 0}}
  .warning{{background:#fffbeb;border:1px solid #f59e0b;
            border-radius:8px;padding:12px 16px;margin:12px 0;
            color:#92400e;font-size:14px}}
  .checkpoint{{background:#f0fdf4;border:1px solid #86efac;
               border-radius:8px;padding:12px 16px;margin:20px 0;
               color:#166534;font-size:14px}}
  .nav{{display:flex;justify-content:space-between;align-items:center;
        margin-top:24px;padding-top:20px;border-top:1px solid #e5e7eb}}
  .nav button{{padding:10px 24px;background:#3b82f6;color:white;
               border:none;border-radius:8px;cursor:pointer;
               font-size:14px;font-family:inherit}}
  .nav button:disabled{{background:#d1d5db;cursor:default}}
  .nav button:hover:not(:disabled){{background:#2563eb}}
  @media(max-width:600px){{.container{{padding:20px}}}}
  @media print{{.nav,.role-tabs{{display:none}}}}
</style></head><body>
<div class="container">
  <div style="display:flex;justify-content:space-between;
              align-items:center;margin-bottom:24px;">
    <h1 style="margin:0;font-size:18px;color:#1e3a5f;">
      {app_name} 튜토리얼</h1>
    <button onclick="window.print()"
            style="padding:6px 14px;background:white;
                   border:1px solid #d1d5db;border-radius:6px;
                   cursor:pointer;font-size:13px;">인쇄</button>
  </div>
  {panel_html}
</div>
<script>
{role_js}
</script></body></html>""", encoding="utf-8")
    print(f"  [생성] {html_path.name}")


## 수정 후 테스트

generator.py 수정 완료 후 아래 명령으로 바로 테스트:

cd C:\Obsidian\GuideBook\GuideBook_생성기
python generator.py PrintFlow both

완료 후:
1. 생성된 파일 3개 확인:
   - apps/PrintFlow/output/PrintFlow_SOP_v1.0.md
   - apps/PrintFlow/output/PrintFlow_SOP_v1.0.html
   - apps/PrintFlow/output/PrintFlow_튜토리얼_v1.0.html
   (기존 파일은 archive/에 백업됨)

2. HTML 파일 내용 일부 출력 (표가 <table>로 변환됐는지 확인):
   grep -n "<table>" "apps/PrintFlow/output/PrintFlow_SOP_v1.0.html"

3. 튜토리얼 HTML에 역할 탭이 있는지 확인:
   grep -n "role-tab\|조교용\|강사용" "apps/PrintFlow/output/PrintFlow_튜토리얼_v1.0.html"

4. 오류 발생 시 오류 메시지 전체 출력하고 한국어로 원인 설명
```

---

## 예상 결과

| 확인 항목 | 기대값 |
|-----------|--------|
| SOP HTML 표 | `<table>`, `<th>`, `<td>` 태그로 렌더링 |
| 튜토리얼 역할 탭 | "조교용" / "강사용" 탭 버튼 존재 |
| 조교 STEP | 새로고침, 계획열람, 출력, 완료 — 4~6개 |
| 강사 STEP | 자료탐색, 목록지정, 계획등록 — 3개 |

---

## 검증 방법

완료 후 생성된 HTML 파일 2개를 브라우저에서 열어서 확인:
```
start apps\PrintFlow\output\PrintFlow_SOP_v1.0.html
start apps\PrintFlow\output\PrintFlow_튜토리얼_v1.0.html
```

확인 포인트:
- SOP: "자주 하는 실수" 섹션이 표 형태로 보이는지
- 튜토리얼: 상단에 "조교용 / 강사용" 탭이 보이는지

결과를 채팅창에 붙여넣기:
```
generator.py 수정 완료. 확인해줘.
[grep 결과 또는 오류 메시지 붙여넣기]
```

---

## 오류 발생 시
```
시행착오 기록 추가해줘.
오류: [메시지]
상황: generator.py 수정 중
```
